apiVersion: apps/v1
kind: Deployment
metadata:
  name: book-aggregator
  labels:
    app: book-aggregator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: book-aggregator
  template:
    metadata:
      labels:
        app: book-aggregator
    spec:
      initContainers:
      - name: wait-for-kafka
        image: busybox:1.35
        command: ['sh', '-c', 'until nc -z -v -w30 kafka-cluster-kafka-bootstrap:9092; do echo "Waiting for Kafka..."; sleep 2; done;']
      containers:
      - name: book-aggregator
        image: book-aggregator
        env:
        - name: KAFKA_BOOTSTRAP_SERVERS
          value: kafka-cluster-kafka-bootstrap:9092