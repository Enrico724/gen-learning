# Documentazione dell'App CLI
## Panoramica

L'app CLI si trova in `apps/cli` all'interno di questa monorepo. Avviala con:

```bash
python apps/cli
```

Consulta la figura per un esempio.

![alt text](cli-screenshot.png)